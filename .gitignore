# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# Salidas del build. `dist`/`dist-ssr` eran de Vite y ya no se generan; se
# dejan un tiempo para que a nadie con el repo viejo se le cuele una carpeta
# antigua en un commit.
dist
dist-ssr
.next
out

# Lo escribe `next build` en cada arranque; no se versiona.
next-env.d.ts

*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

node_modules/
.cache/

# package-lock.json SÍ se versiona: el Dockerfile usa `npm ci`, que exige el
# lockfile y es lo que hace que el build del servidor instale exactamente las
# mismas versiones que tú tienes en local.

.claude